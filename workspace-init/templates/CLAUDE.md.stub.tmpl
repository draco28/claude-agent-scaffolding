# ${PROJECT_NAME} — AI workspace

This is the AI workspace half of a dual-repo project.
Paired canonical repo: `${CANONICAL_ROOT}`.

## What lives here

- `.claude/memory-bank/` — long-running notes scaffold-onboard authors (after `/onboard`)
- `docs/MASTER-SPEC.md` — single source of truth for the project (scaffold-onboard authors)
- `docs/specs/` — sprint specs (scaffold-dev authors)
- `.workspace/pairing.json` — pairing manifest (workspace-init wrote this)
- `.workspace/init-log` — workspace-init's rollback record

## What lives in canonical (`${CANONICAL_ROOT}`)

- `src/` — production code
- `tests/` — production tests
- `docs/PRD.md`, `docs/SRS.md`, `docs/BACKLOG.md` — product docs (scaffold-onboard authors)
- `docs/adr/` — product ADRs

## Next steps

If memory-bank is empty, run `/onboard` to start scaffold-onboard's 10-phase conversation.
If you already onboarded, the memory-bank should hydrate your session automatically.

## AI-trace filter

A `commit-msg` hook is installed in both this AI workspace and the canonical repo. It blocks:
- `Co-Authored-By:` trailers
- `🤖 Generated with` markers
- `<noreply@anthropic.com>` / `<noreply@openai.com>` references

Override with `git commit --no-verify` only if you understand the implication.

---

(This file was generated by workspace-init v0.1.0. scaffold-onboard's `/scaffold-project` will overwrite it with a richer router once you onboard the project.)
