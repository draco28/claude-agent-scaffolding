{
  "control": "old-contract detection control",
  "surface": "external-executor",
  "fixture": "02-external-request-result-handling",
  "why_this_fixture": "The most discriminating of the seven: it is the core in-session contract, so an invoke agent reading prose that predates the seam cannot answer it correctly by accident. The negative control 01 is deliberately NOT used here - it would pass against the old prose, which is what makes it a control rather than a discriminator.",
  "pre_change_sha": "3e3a2f3",
  "pre_change_prose": [
    "ossify/skills/work-item/SKILL.md",
    "ossify/skills/work-item/references/round-orchestration.md",
    "ossify/skills/close/references/work-item-close.md",
    "ossify/commands/run-spine.md"
  ],
  "note_on_prose_set": "references/external-executor.md and references/correction-continuation.md do not exist at 3e3a2f3; their absence is the change under test.",
  "must_fail": true,
  "observed_pass": false,
  "result": {
    "scores": {
      "mode_split": 1,
      "round_sequencing": 1,
      "result_validation": 1,
      "gaps_routing": 2,
      "correction_and_layer4": 4
    },
    "pass": false,
    "notes": "Output asserts --external-executor is undefined by the authority docs (missing skills/work-item/references/external-executor.md, which exists and specifies the mode) and instead executes the nested Task-dispatch path throughout, so mode_split, round_sequencing, and result_validation all describe the wrong mode's mechanics rather than the external_execution_request/result contract, and gaps_routing invents nested-mode redispatch instead of staying silent or routing via the caller; correction_and_layer4 is untouched by the fixture and the output invents nothing there, so it caps at 4."
  },
  "verdict": "DETECTS - control failed as required, and sharper than the previous round: gaps_routing moved 4 -> 2 now that criterion 4 describes a separate gaps RECORD, so the old prose's nested-redispatch framing is a positive wrong answer rather than mere silence.",
  "rerun_reason": "Round-two restructure (R2-1) split section 5 into 5a/5b and rewrote rubric criteria 3 and 4, so the previous control no longer evidences THIS rubric. Re-run against the same 3e3a2f3 prose with fresh invoke and judge agents."
}
