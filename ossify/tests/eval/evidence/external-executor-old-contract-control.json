{
  "control": "old-contract detection control",
  "surface": "external-executor",
  "fixture": "02-external-request-result-handling",
  "why_this_fixture": "The most discriminating of the seven: it is the core in-session contract, so an invoke agent reading prose that predates the seam cannot answer it correctly by accident. The negative control 01 is deliberately NOT used here - it would pass against the old prose, which is what makes it a control rather than a discriminator.",
  "pre_change_sha": "3e3a2f3",
  "pre_change_prose": [
    "ossify/skills/work-item/SKILL.md",
    "ossify/skills/work-item/references/round-orchestration.md",
    "ossify/skills/close/references/work-item-close.md",
    "ossify/commands/run-spine.md"
  ],
  "note_on_prose_set": "references/external-executor.md and references/correction-continuation.md do not exist at 3e3a2f3; their absence is the change under test.",
  "must_fail": true,
  "observed_pass": false,
  "result": {
    "scores": {
      "mode_split": 1,
      "round_sequencing": 1,
      "result_validation": 1,
      "gaps_routing": 1,
      "correction_and_layer4": 4
    },
    "pass": false,
    "notes": "Output declares --external-executor undocumented and walks the ordinary nested Task-dispatch path instead, so it never builds an external_execution_request or invokes a caller procedure (round_sequencing=1), asserts the only pre-close check is the mode enum with no fingerprint recompute (result_validation=1), frames gaps-surfaced as re-dispatch to the same subagent worker (gaps_routing=1), and collapses external into the default nested mode exactly as the rubric names as a wrong answer (mode_split=1); correction_and_layer4 is capped at 4 since the scenario's accepted-result walk never exercises rejection/close and the output correctly stays silent there rather than inventing facts."
  },
  "verdict": "DETECTS - control failed as required, and the corrected gaps_routing criterion now catches the old prose's re-dispatch framing too (1, previously capped at 4 as unexercised).",
  "rerun_reason": "Rubric criteria 3 and 4 changed in the F5 fix round, so the previous control no longer evidences THIS rubric. Re-run against the same 3e3a2f3 prose with fresh invoke and judge agents."
}
