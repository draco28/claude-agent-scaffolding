#!/usr/bin/env bash
# scaffold MCP server launcher — implemented in Phase F.
# Phase A: not yet functional. Lazy-launched by Claude Code; will exit
# immediately so no MCP tools are registered until Phase F lands.
echo "scaffold-memory MCP server is not yet implemented (Phase F pending)" >&2
exit 1
