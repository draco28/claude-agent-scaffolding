"""scaffold-memory MCP server — Phase A skeleton.

This module is implemented in Phase F. It will expose 9 MCP tools backed by
SQLite + sqlite-vec + Ollama (with FTS5 fallback). See docs/SPEC-scaffold.md §5.5.

For Phase A the file exists so the package layout is valid; running it is
not yet supported.
"""

if __name__ == "__main__":
    raise SystemExit("Phase F not yet implemented")
