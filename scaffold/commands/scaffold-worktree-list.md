---
description: List all worktrees of the current repo with branch, current slice, phase, last activity.
argument-hint: ""
allowed-tools: Bash(bash:*)
---

Run \`git worktree list\` and join with each branch's state.json.

> **Build status:** stub. Implementation lands in Phase G. The plugin
> registers this command on install but it is not yet functional.
