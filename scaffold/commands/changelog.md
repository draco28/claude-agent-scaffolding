---
description: Append an entry to CHANGELOG.md (Keep-a-Changelog). Without args: prompts for type + summary. With \`bump\`: rotates Unreleased to a versioned entry.
argument-hint: "[bump]"
allowed-tools: Bash(bash:*)
---

Creates CHANGELOG.md if missing. See SPEC \`§5.3\`.

> **Build status:** stub. Implementation lands in Phase E. The plugin
> registers this command on install but it is not yet functional.
