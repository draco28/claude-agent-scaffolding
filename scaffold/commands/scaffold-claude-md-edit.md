---
description: Open the personal-defaults or project-specific CLAUDE.md source layer in $EDITOR; regenerate <repo>/CLAUDE.md on save.
argument-hint: "personal|project"
allowed-tools: Bash(bash:*)
---

Open the requested source layer in \$EDITOR. On save, run the generator. See SPEC \`§5.4\`.

> **Build status:** stub. Implementation lands in Phase C. The plugin
> registers this command on install but it is not yet functional.
