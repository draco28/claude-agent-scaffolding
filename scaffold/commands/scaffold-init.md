---
description: Bootstrap or onboard the current repo with sensible defaults (LICENSE, .gitignore, README, CLAUDE.md, docs structure).
argument-hint: ""
allowed-tools: Bash(bash:*)
---

Detect the current repo state (fresh / existing-unmanaged / scaffold-managed) and act accordingly. See SPEC \`§5.1\`.

> **Build status:** stub. Implementation lands in Phase C. The plugin
> registers this command on install but it is not yet functional.
