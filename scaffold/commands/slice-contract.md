---
description: Enter the contract phase: scaffold failing tests for each acceptance criterion.
argument-hint: ""
allowed-tools: Bash(bash:*)
---

Gate: spec phase complete with ≥1 AC. Detects test framework via stack signals. Refuses if framework cannot be detected (OQ-2).

> **Build status:** stub. Implementation lands in Phase D. The plugin
> registers this command on install but it is not yet functional.
