---
description: Enter the scaffold phase: write Zone-1 boilerplate (skeletons, types, glue). Tests still failing afterwards.
argument-hint: ""
allowed-tools: Bash(bash:*)
---

Gate: contract phase complete with ≥1 failing test.

> **Build status:** stub. Implementation lands in Phase D. The plugin
> registers this command on install but it is not yet functional.
