#!/usr/bin/env bash
# scaffold/scripts/install.sh — bootstraps the Python venv for the MCP server.
# Phase A: stub. Phase F replaces with full install logic (venv create,
# pip install -r mcp/requirements.txt, Ollama health check).

echo "scaffold install.sh: not yet implemented (Phase F pending)" >&2
exit 1
