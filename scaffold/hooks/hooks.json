{
  "description": "scaffold hooks: SessionStart injects project context (current slice, phase, llm flag, recent decisions) when the current dir is scaffold-managed. Source-aware (mirrors ai-mentor v1.1 pattern) so context survives compaction.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks-handlers/session-start.sh\""
          }
        ]
      }
    ]
  }
}
