<!-- vars:
  sprint_id                   e.g. sprint-3
  phase_id                    e.g. Phase 3
  start_date                  YYYY-MM-DD
  end_date                    YYYY-MM-DD
  slices_delivered_count      integer
  slices_planned_count        integer
  carry_forward_in            handoff filename consumed from sprint(N-1)
  carry_forward_out           handoff filename authored for sprint(N+1)
  sprint_goal_text            verbatim goal from ROADMAP.md
  delivered_summary           prose describing what shipped + variances
  per_slice_rollup_table      markdown table: slice | items | rounds | auto | manual | critic | fixups
  cross_slice_patterns_block  prose describing 0-3 patterns observed across slices
  memory_bank_impact_totals   bulleted totals: accepted / backlog / rejected / deferred
  lessons_for_next_sprint     bulleted lessons with citations to slice + observation
  reference_index             bulleted list (slice retros, ADRs authored, carry-forward path)
-->
# {{sprint_id}} retrospective

## 1. Sprint metadata

- Sprint: {{sprint_id}}
- Phase: {{phase_id}}
- Duration: {{start_date}} -> {{end_date}}
- Slices delivered: {{slices_delivered_count}}
- Slices planned: {{slices_planned_count}}
- Carry-forward consumed: {{carry_forward_in}}
- Carry-forward authored: {{carry_forward_out}}

## 2. Sprint goal vs delivered

**Planned goal:** {{sprint_goal_text}}

{{delivered_summary}}

## 3. Per-slice rollup

{{per_slice_rollup_table}}

## 4. Cross-slice patterns

{{cross_slice_patterns_block}}

## 5. Memory bank impact totals

{{memory_bank_impact_totals}}

## 6. Lessons for next sprint

{{lessons_for_next_sprint}}

## 7. Reference index

{{reference_index}}
