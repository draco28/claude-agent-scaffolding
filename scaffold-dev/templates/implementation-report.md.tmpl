<!-- vars:
  work_item_id          e.g. work-3.2.01
  vs_id                 e.g. VS-3.2
  vs_kebab              e.g. insights-action-needed-card
  round_id              e.g. R1
  worktree_abs_path     absolute path to canonical worktree
  branch_name           e.g. work/3.2.01-backend-query-endpoint
  summary_paragraph     2-4 sentences summarizing what was implemented and why
  acs_status_table      markdown table: AC | status (passed/partial/deferred) | evidence
  files_changed         bulleted list with file path + +added/-removed line counts
  verification_run      fenced block showing exact commands + per-command result
  decisions_during_exec bulleted in-band judgement calls made during execution
  deviations_from_spec  bulleted spec divergences (or "None.")
  memory_bank_sugs      bulleted suggestions for memory-bank promotion at slice-close harvest
  orchestrator_notes    bulleted notes for the orchestrator's verification + commit step
-->
# Implementation report — {{work_item_id}}

## 1. Work item

- ID: {{work_item_id}}
- Slice: {{vs_id}} ({{vs_kebab}})
- Round: {{round_id}}
- Worktree: {{worktree_abs_path}}
- Branch: {{branch_name}}

## 2. Summary

{{summary_paragraph}}

## 3. ACs — verification status

{{acs_status_table}}

## 4. Files changed

{{files_changed}}

## 5. Verification commands run

{{verification_run}}

## 6. Decisions during execution

{{decisions_during_exec}}

## 7. Deviations from spec

{{deviations_from_spec}}

## 8. Suggestions for memory bank

{{memory_bank_sugs}}

## 9. Notes for orchestrator

{{orchestrator_notes}}
