<!-- vars:
  handoff_type                forward | return
  scope                       sprint | slice | mid-slice | bugfix | techdebt
  scope_specifier             e.g. "VS-3.2 in sprint-3", "sprint-3 closing -> sprint-4 starting"
  purpose_slug                e.g. bugfix-auth, context-bloat, 3-to-4
  short_id                    4-char hex; matches paired forward/return
  source_session_metadata     authoring session description + timestamp + context-budget usage
  references_forward_handoff  filename of the paired forward handoff (return only; "n/a" for forward)
  purpose_paragraph           one paragraph explaining why this handoff exists
  state_pointers_block        bulleted: workspace paths, sprint/slice IDs, worktrees, branches, commits
  not_in_memory_bank_block    section 4 -- the ephemeral pre-codification value-add content
  workflow_deviations         bulleted deviations from standard scaffold-dev workflow (or "None.")
  in_flight_state_block       bulleted: open work items, partial commits, dispatched-not-returned subagents
  must_read_before_doing      bulleted list of specific files beyond Tier 0 auto-load
  next_intended_actions       single action OR ranked list of options the consuming session should take
  anti_actions_block          bulleted "do NOT do X" warnings
  return_template_stub        section 10 -- only filled for forward handoffs expecting a return
-->
# Handoff — {{scope}}-{{purpose_slug}}

## 1. Header

- Type: {{handoff_type}}
- Scope: {{scope}} ({{scope_specifier}})
- Purpose: {{purpose_slug}}
- Short-id: {{short_id}}
- References forward handoff: {{references_forward_handoff}}
- Source session metadata: {{source_session_metadata}}

## 2. Purpose

{{purpose_paragraph}}

## 3. State pointers

{{state_pointers_block}}

## 4. What's NOT in memory bank yet

{{not_in_memory_bank_block}}

## 5. Workflow deviations

{{workflow_deviations}}

## 6. In-flight state

{{in_flight_state_block}}

## 7. Must read before doing anything

{{must_read_before_doing}}

## 8. Next intended action(s)

{{next_intended_actions}}

## 9. Anti-actions

> **Banned operator-facing commands.** Do NOT instruct the next session or operator to run `git stash` / `git stash pop` / `git stash apply` for baseline isolation — it collides with the operator's pre-existing stash stack and can be unrecoverable. Use a reversible file move instead (`mv <file> /tmp/<slice-id>-baseline/<file>`, reverse on rollback), visible in `ls /tmp/`.

{{anti_actions_block}}

## 10. Return-handoff template stub

{{return_template_stub}}
