<!-- vars:
  work_item_id           e.g. work-3.2.01
  work_item_title        short kebab-or-prose title
  vs_id                  e.g. VS-3.2
  vs_kebab               e.g. insights-action-needed-card
  round_id               e.g. R1
  worktree_abs_path      absolute path to the canonical worktree for this work item
  branch_name            e.g. work/3.2.01-backend-query-endpoint
  context_paragraph      1-3 sentences linking this work item to the slice goal
  decisions_baked_in     bulleted decisions pre-pinned by spec authoring + audit
  traceability_block     FR/NFR/BACKLOG IDs inherited from the ROADMAP vertical slice
  files_to_modify        bulleted list of paths (added | modified)
  acs_block              §14.1 machine-checkable AC lines the gate parses AND runs. Each auto line: "- [ ] AC-<n> auto: `<command>` -> expected: <exit 0 | exit N | output contains <text>>" (use a real number for <n>). The command MUST be backtick-wrapped (sd_verify_auto_step extracts it from the backticks); each AUTO line MUST carry a real AC-<n> label (sd_verify_report_cross_check keys off it); 'output contains' takes the substring UNQUOTED (quotes become part of the needle). Manual demo steps: "- [ ] user: <manual demo step>" — NO AC id (user rows are verified at slice-close, not report-cross-checked).
  verification_block     fenced bash block listing executable verification commands
  demo_contribution      one paragraph: how this work item advances slice demo criteria
  not_in_scope           bulleted list of out-of-scope items (anti-actions)
  reference_index        bulleted list of reference paths (spec sections, ADRs, memory bank)
-->
# {{work_item_id}} — {{work_item_title}}

## 1. Header

- Work item: {{work_item_id}}
- Vertical slice: {{vs_id}} ({{vs_kebab}})
- Round: {{round_id}}
- Worktree (absolute path): {{worktree_abs_path}}
- Branch: {{branch_name}}

## 2. Context

{{context_paragraph}}

## 3. Decisions baked in

{{decisions_baked_in}}

## 4. Traceability

{{traceability_block}}

## 5. Files to modify

{{files_to_modify}}

## 6. Acceptance criteria (machine-checkable)

> Authoritative AC source of truth — `implementation-checking` parses **and runs** these lines.
> Each AC is a numbered checklist row: `- [ ] AC-<n> auto:` then the command **wrapped in backticks**,
> then `→ expected:` and one of `exit 0` / `exit N` / `output contains <text>` (substring **unquoted**).
> The verifier extracts the command from the backticks and the report cross-check keys off the real
> `AC-<n>` ids, so **both are required**. `user:` lines (`- [ ] user: <step>`, **no** AC id) are manual
> demo steps, verified at slice close. (Author real numbers — `AC-1`, `AC-2`, … — on the auto rows below.)

{{acs_block}}

## 7. Verification (executable)

{{verification_block}}

## 8. Demo contribution

{{demo_contribution}}

## 9. Not in this work item

{{not_in_scope}}

## Reference index

{{reference_index}}
