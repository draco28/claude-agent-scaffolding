---
description: Exit spotter mode — pure delegation. AI does the work. Hook is no-op.
argument-hint: ""
allowed-tools: Bash(bash:*)
---

Run the following command to flip AI Mentor state to zone=1 (no enforcement):

```bash
bash -c 'source "${CLAUDE_PLUGIN_ROOT}/lib/state.sh" && am_set_zone 1 null slash:/z1 && echo "AI Mentor: zone=1 (delegation). Hook is no-op. AI works freely."'
```

After running, briefly confirm to the user that they are now in zone 1 and that you will work directly on tasks without spotter friction. No need to announce zones for subsequent edits unless the user re-engages a Curve 2 mode.
